name: Schedule Test

on:
  workflow_dispatch:
  schedule:
    - cron: "30 7 * * *"   # 1:00 PM IST daily

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: |
          date
          echo "Triggered by ${{ github.event_name }}"
